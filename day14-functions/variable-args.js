function add(n1,n2) {
    console.log(arguements);
    console.log(n1,n2);

}
add(10,20);
// if less args are passed, by default the parameters will hold undefined
add(10);
// extra args are neglected
add(10,20,30);